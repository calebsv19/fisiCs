#ifndef UTILS_H
#define UTILS_H

// Toggles printing the full source buffer after loading
#define DEBUG_PRINT_SOURCE 1

// Reads a file's content into a null-terminated string
char* readFile(const char* filename);

#endif // UTILS_H

