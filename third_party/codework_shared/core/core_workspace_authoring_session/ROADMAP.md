# Roadmap

- 0.1.x: establish the host-neutral session lifecycle and prove it with the
  Workspace Sandbox live-shared integration.
- Later: add optional typed diagnostics only when multiple hosts need the same
  structured failure vocabulary.
