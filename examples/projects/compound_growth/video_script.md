# Video Script: Compound Growth

1. Open `fisiCs/examples/projects/compound_growth` in the IDE.
2. Show `src/main.c` and the tiny project structure.
3. Run `make examples-project NAME=compound_growth`.
4. Point out the deterministic tick/balance output.
5. Change one behavior rule, such as contribution amount or tick count.
6. Rerun the project and show the expected-output check catching any mismatch.
7. Generate artifacts with `make examples-project-artifacts NAME=compound_growth`.
8. Show the generated `ide_files/build_graph.json` as the bridge into
   IDE/tooling workflows.
