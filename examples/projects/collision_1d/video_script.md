# Video Script: Collision 1D

1. Open `fisiCs/examples/projects/collision_1d` in the IDE.
2. Show mass, velocity, momentum, and energy annotations in `src/main.c`.
3. Run `make examples-project NAME=collision_1d`.
4. Show the deterministic post-collision table.
5. Run `make examples-project-invalid NAME=collision_1d`.
6. Point out the units mismatch and skipped code generation.
7. Generate artifacts with `make examples-project-artifacts NAME=collision_1d`.
8. Use IDE units/diagnostic views once available to connect the fixture to the
   compiler analysis.
