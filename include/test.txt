#include <stdbool.h>
#include <stdint.h>
#include <limits.h>
#include <stddef.h>

int main(void) {
    bool ok = true;
    int32_t v = INT32_MAX;
    size_t z = sizeof(v);
    return ok ? (int)(v == INT32_MAX) + (int)z + (INT_MAX > 0) : 0;
}
