long long w18_deep_leaf(long double ld, int i, unsigned u) {
    long long a = (long long)(ld * 16.0L);
    long long b = (long long)i * 10ll;
    long long c = (long long)u * 7ll;
    return a + b + c;
}
