typedef enum { WAVE50_LEFT = 1 } Wave50Left;
typedef enum { WAVE50_RIGHT = 2 } Wave50Right;

void wave50_distinct(Wave50Left mode);
void wave50_distinct(Wave50Right mode);

int main(void) {
    return 0;
}
